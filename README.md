# 📸 CSS Stories Menu (Instagram-style)

A sleek **HTML + CSS project** that recreates an Instagram-inspired **stories menu**. Each story is represented by a circular profile image with different states (new, viewed, live), styled using gradients, colors, and pseudo-elements.

## ✨ Features
- **Story States**  
  - **New** → Gradient border (Instagram-style)  
  - **Viewed** → Gray border  
  - **Live** → Red “LIVE” badge overlay  

- **Profile Layout**  
  - Circular profile images inside `.img-wrapper`  
  - User names displayed below each story  

- **Customizable Variables**  
  - Colors, gradients, and fonts defined in `:root` for easy theme adjustments  
  - Font: *Karla* from Google Fonts  

- **Responsive Flexbox Design**  
  - Stories displayed in a horizontal row with spacing  
  - Centered and compact layout  

- **Minimal Structure**  
  - Simple `<ul>` list with `<li>` items for each story  
  - Clean separation of HTML structure and CSS styling  

## 🛠️ Tech Stack
- **HTML5**  
- **CSS3 (Flexbox + Variables + Pseudo-elements)**  

## 🚀 Use Cases
- Social media UI mockups  
- Portfolio projects showcasing CSS creativity  
- Practicing gradients, pseudo-elements, and responsive design  
- Building reusable UI components for dashboards or apps  

## 📂 Project Structure
```
index.html   # Main HTML structure
style.css    # Stories menu styling and states
```
